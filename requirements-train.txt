# GPU training only (Colab). Kept minimal on purpose: Colab already ships torch,
# and a long dependency list is a long list of things that can break the install.
transformers>=4.40
datasets>=2.18
Pillow
accelerate
mlflow
